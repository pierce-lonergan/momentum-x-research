# ╔══════════════════════════════════════════════════════════════╗
# ║              MOMENTUM-X CONFIGURATION                       ║
# ║  Copy this file to .env and fill in your keys               ║
# ║  cp .env.example .env                                       ║
# ╚══════════════════════════════════════════════════════════════╝

# ── LEVEL 1: No keys needed ─────────────────────────────────
# Just run tests: make test
# No API keys required for unit tests.

# ── LEVEL 2: Paper Trading (Recommended Start) ──────────────

# Alpaca API (https://app.alpaca.markets → Paper Trading → API Keys)
ALPACA_API_KEY=your_paper_api_key_here
ALPACA_SECRET_KEY=your_paper_secret_key_here
ALPACA_BASE_URL=https://paper-api.alpaca.markets
ALPACA_DATA_URL=https://data.alpaca.markets
ALPACA_FEED=sip                  # "sip" (paid, recommended) or "iex" (free, limited)

# LLM Provider (pick ONE — Together AI is cheapest)
#
# Option A: Together AI (https://together.ai — $25 free credits)
TOGETHER_AI_API_KEY=your_together_key_here
# D77: Kimi K2 Thinking — best reasoning model on Together AI ($1.20/$4.00)
LLM_TIER1_MODEL=moonshotai/Kimi-K2-Thinking
LLM_TIER1_PROVIDER=together_ai
# D77: Kimi K2.5 — multimodal, 262K context, JSON mode ($0.50/$2.80)
LLM_TIER2_MODEL=moonshotai/Kimi-K2.5
LLM_TIER2_PROVIDER=together_ai
#
# Option B: OpenRouter (https://openrouter.ai — pay-per-token)
# OPENROUTER_API_KEY=your_openrouter_key_here
# LLM_TIER1_MODEL=deepseek-ai/DeepSeek-R1
# LLM_TIER1_PROVIDER=openrouter
# LLM_TIER2_MODEL=qwen/qwen-2.5-14b-instruct
# LLM_TIER2_PROVIDER=openrouter
#
# Option C: Fireworks AI (https://fireworks.ai)
# FIREWORKS_AI_API_KEY=your_fireworks_key_here
# LLM_TIER1_PROVIDER=fireworks_ai
# LLM_TIER2_PROVIDER=fireworks_ai

# LLM Settings
LLM_DEFAULT_TEMPERATURE=0.3
LLM_MAX_TOKENS=4096
LLM_TIMEOUT_SECONDS=120

# ── LEVEL 3: Live Trading (DANGER ZONE) ─────────────────────
# ⚠️  Only change these if you understand the risks!
# ⚠️  Default is PAPER trading. You MUST explicitly enable live.
#
# ALPACA_API_KEY=your_LIVE_api_key_here        # Different key!
# ALPACA_SECRET_KEY=your_LIVE_secret_key_here   # Different key!
# ALPACA_BASE_URL=https://api.alpaca.markets    # Live endpoint

# ── Scanner Thresholds ──────────────────────────────────────
# These defaults are research-derived. Override only if you know why.
# See: docs/mathematics/MOMENTUM_LOGIC.md
#
# SCAN_RVOL_PREMARKET_MIN=2.0        # Minimum relative volume
# SCAN_RVOL_INTRADAY_MIN=3.0         # Stricter for intraday
# SCAN_GAP_PCT_MIN=0.05              # 5% minimum gap
# SCAN_GAP_PCT_EXPLOSIVE=0.20        # 20% explosive gap
# SCAN_ATR_RATIO_MIN=1.5             # ATR ratio threshold
# SCAN_FLOAT_MAX_SHARES=20000000     # Max float (20M shares)
# SCAN_PRICE_MIN=0.50                # Min stock price
# SCAN_PRICE_MAX=20.00               # Max stock price

# ── Execution ───────────────────────────────────────────────
# Competition mode (D37): larger positions, more trades, tighter stops
# EXEC_MAX_POSITIONS=8               # Max concurrent positions (default: 8)
# EXEC_MAX_POSITION_PCT=0.15         # 15% of portfolio per trade (default: 15%)
# EXEC_STOP_LOSS_PCT=0.04            # 4% stop-loss (default: 4%)
# EXEC_DAILY_LOSS_LIMIT_PCT=0.10     # -10% circuit breaker (default: 10%)

# ── Scoring Weights ─────────────────────────────────────────
# Must sum to 1.0. See: docs/mathematics/MOMENTUM_LOGIC.md §5
# SCORE_CATALYST_NEWS=0.30
# SCORE_TECHNICAL=0.20
# SCORE_VOLUME_RVOL=0.20
# SCORE_FLOAT_STRUCTURE=0.15
# SCORE_INSTITUTIONAL=0.10
# SCORE_DEEP_SEARCH=0.05

# ── Debate Engine ───────────────────────────────────────────
# DEBATE_DIVERGENCE_HIGH_THRESHOLD=0.6
# DEBATE_DIVERGENCE_LOW_THRESHOLD=0.3
# DEBATE_MFCS_DEBATE_THRESHOLD=0.6

# ── Finnhub (Optional — enriches news) ─────────────────────
# FINNHUB_API_KEY=your_finnhub_key_here
