# 274 — THE MUTATION GAUNTLET: the capability curve of the falsification engine

**Author**: Claude (Fable 5 era) | **Date**: 2026-06-11 (pre-dawn → day) | **Class**: DORMANT-C (research; read-only vs production; the deployed tree untouched; gauntlet compute capped at 8 workers) | **Charter**: doc-274 mandate, `/ultracode` throughout. **§3 (pre-registration) is committed BEFORE any gauntlet cell runs** — the git history is the audit trail, as in 273. Results and the mandated hostile retest follow in separate commits.

> 273 measured what the tape can know. 274 measures what the engine can certify.

---

## §1 — The idea generation, shown

Eight candidates graded before any apparatus ran (full table in `data/research/doc274_state.md`, written 04:27–04:45): the engine-epistemology probe (**chosen**); the shape-of-unpredictability taxonomy (rejected: surrogate-data methods are artifact-prone on halt-broken microcap tapes — the conclusion would be methodology residue our own retest would kill); the signed/unsigned information decomposition (rejected: "273's F4 with error bars"); fleet-level FDR (partially absorbed — its sound core, the substrate's immune system, is this experiment's deliverable; full FDR calibration chartered as Stage 2); causal inference on the randomized live arms (rejected for tonight: ~2 weeks of trades per arm; unlock at ≥150 trades/arm); the bounded generative probe (rejected: half-run risk, unsharp stepping stone); the trust-propagation graph (absorbed: it *is* the capability matrix's implication layer); cascade anti-selection (unlock not met: ~10 of ≥40 clean days).

**Two apparatuses ran before this prereg froze, and both changed it:**

1. **Deep-research novelty sweep** (`wf_11184ab9-a78`: 106 agents, 117 claims extracted, 22 verified 3-0, 3 over-strong claims refuted 0-3). Verdict: **the composite is genuinely novel.** Every ingredient has mature adjacent precedent — DeepMutation (the canonical mutation-testing transplant to ML), LIGO hardware/software/blind injections (the "Big Dog" full-workflow exercise), ERCC spike-ins (whose Latin-square fold-change design IS a graded detection-capability curve), Kepler transit injection-recovery (per-pipeline-stage attribution at fixed false-alarm rate — the closest analytic ancestor of this capability matrix), proteomics target-decoy FDR, and finance's own CSCV/PBO power-tested on certified-null vs planted-signal series — but no located work injects *statistical-research pathologies* (look-ahead, survivorship, membership contamination, universe drift, null mis-specification) into a complete falsification stack; **Angle 5 (LLM/agent referees as measured defect detectors) returned zero prior art anywhere**; and Angle 4 (the multi-detector × multi-fault grading framework) is the thinnest verified record of all — meaning the expected-outcome-matrix design below is itself new methodology.
2. **Design-stress panel** (`wf_8c00cf6d-1d6`: five hostile attackers + synthesizer, run against the draft taxonomy BEFORE this prereg — an adversarial stage 273 never had). It found a structural flaw that would have invalidated the headline: **injecting at the spine level while D2/D4 audit the raw warehouse makes every D2/D4 catch a tautology** ("code bug" geometry), when the class that actually bit this project (2026-03-23) was *the warehouse agreeing with the lie*. The panel's restructure is adopted wholesale and constitutes most of §3. One attacker ran the draft arms empirically: the τ=600s look-ahead arm produces dll **+0.0160** (harm real, mechanism = entry-window visibility), τ=30s is inert (25s of a 295s window), and Bernoulli survivorship **cannot** produce a false positive on a null spine (measured −0.006 at π=0.30; even the intensity-collider variant −0.0036) — so P4's harm lives in the economics channel, and those design-phase measurements are disclosed here: **the harm magnitudes of P2-ENTRYVIS and P4 are design-known; their cells are confirmatory for the DEFENSE response and margins only.**

## §2 — The hard problem, stated precisely

The project's falsification stack — pre-registration, within-day permutation nulls with maxT, winsorization, cross-regime splits, planted-signal MDE, membership audits, adversarial referee fleets — has exactly one measured data point about its own reliability: it caught one contamination, once, at the agent layer, after the mechanical layers had certified the result. **Nothing is known about which pathology classes each layer can detect, at what severity, with what false-fire rate — so nothing quantified is known about what any prior null or discovery actually certifies.** The experiment: inject a mechanism-typed, graded-severity pathology taxonomy into a reduced surrogate of the doc-273 engine running on the certified-null clean universe, through a **dual-mount overlay** so that "the source itself lies" (the real class) is measured separately from "the pipeline diverges from a true source" (the tautology class); instrument nine defense layers including, under a sealed instance-blinded protocol, the LLM referee fleet itself; validate the injector against the real wild specimen; and report the capability matrix against a **frozen expected-outcome matrix** where the single confirmatory statistic is the *surprise count*. Why harder than 273: the object of study is the methodology; injections must be faithful (not strawmen — the panel killed three of mine); detection criteria must be non-circular (auditors graded on classes they were not designed for); the inference is a battery-on-battery problem with no published framework; and a wrong design here doesn't just produce a wrong number — it forges a trust certificate for every past and future result.

## §3 — PRE-REGISTRATION (frozen before any cell runs; procedure = `scripts/mutation_gauntlet_doc274.py` + `scripts/doc274_cells_registry.py`, committed with this section)

**Base data & membership burden (273's rule, discharged):** the doc-273 CLEAN spine (membership-audited, lag≤4d), reduced to the 5-point grid {9:40, 10:30, 12:00, 14:00, 15:00}; the engine is the 273 registered statistic (logit+GBM dll, θ=1%, 6 day-block folds) — **labelled a "reduced surrogate" in every claim** (5-pt grid, B=60/200 nulls; the bridge to the full engine is the 273 run itself). The real specimen (contaminated spine) is the positive control and is **forbidden as an injection donor**. Injected cohorts are built from raw warehouse rows through the real gate SQL with full provenance recorded. Injector fidelity: zero-knob identity check (**PASSED pre-commit: 125 rows, 0 mismatches**) + the specimen-reproduction criteria below.

**Dual-mount overlay (the panel's structural repair):** hole-type mutations are date-masks applied to `day_aggs` queries. *Overlay-mount* = the audits apply the same mask (poisoned source — **the registered capability number**); *truth-mount* = audits see the unmasked warehouse (code-bug variant — reported separately as a regression test). Frozen masks: HOLE-REPLAY deletes 2025-09-15..2025-12-05 (12-week hole; stale-prev-close geometry replayed on a different hole than the wild one); ADV-HOLE deletes 2025-10-13..2025-11-07 with qualification window 2025-11-17..2025-12-31 (prev close FRESH; the 20-row ADV window silently reaches back across the hole — the freshness rule is structurally blind by design of the *pathology*, not the test).

**The frozen cell grid** (drop-order if the clock bites, last-to-first: P5_DRIFT, P4_SURV, P3_MIX01, P1_HOLEREPLAY, then D9 demotes to specimen+decoys):

| cell | mechanism | null | expected (frozen predictions) |
|---|---|---|---|
| CLEAN_A | none (shared B=200 null) | shared | all defenses SILENT (specificity) |
| CLEAN_B | none (fresh B=60 null, end-to-end) | fresh | all SILENT |
| SPECIMEN | real 2026-03-23 zombies | fresh | D1 FIRE (harm), D2b-freshness FIRE *(designed/regression)*, D3 FIRE (~92% share), D4 SILENT, D5 FIRE (both-halves-neg), D7-recovered FIRE (.012>.0113), D7-oracle SILENT (.012<.0169), D8 SILENT (≈2.4pt pooled shift < 5pt band), D10 FIRE-or-borderline |
| P1_ADVHOLE | ADV-window hole; gate true, source lies | fresh | **FOREIGN headline cell**: D2-overlay SILENT (the hole demonstrated), D2-truth FIRE (regression), D1/D3 fire iff potency oracle materializes, D10 possible |
| P1_HOLEREPLAY | 12-wk hole replay, stale prev close | fresh | D2b-overlay FIRE (**the freshness rule generalizes** — capability, not regression), D1/D3 per potency |
| P2_ENTRYVIS | features see t+600s (harm known +0.016) | shared | D1 FIRE, D4 FIRE *(designed)*, D7-recovered FIRE, D7-oracle ~SILENT, D3 SILENT (dispersed), D5 SILENT (both halves positive) |
| P3_MIX03 | exit-statistic mix λ=0.3 (39th col) | fresh (spot-check of null-sharing) | D1 FIRE, D4 SILENT *(structurally blind — sampled-coverage disclosure)*, D7 both FIRE, D3 SILENT |
| P3_MIX01 | λ=0.1 | shared | D1 FIRE (smaller), D7-recovered borderline, D7-oracle SILENT |
| P4_SURV | drop losing ticker-days π=0.30 | fresh | D1 SILENT (design-known), **D8 FIRE** (base ≈+8pt > 5pt band), valid_n shrink visible |
| P5_DRIFT | drop 50% of H2 high-ADV (gate stays true) | fresh | D1 SILENT, D2 SILENT, **D10 FIRE** *(designed — built tonight)*, D8 possible |
| P6_NULLFID | 273-original asymmetric null vs mirrored, paired | — | tail-deflation > 0 (anti-conservative), cross-t coupling diagnostic separates them (measurement cell, not fire/silent) |

**Defenses & frozen thresholds:** D1 maxT p<0.05 (records harm for FP classes); D2 gate-recompute + freshness (≤4d), per mount; D3 max single-day share of positive per-row dll mass >50% **and** total mass > the clean-cells' max (3-point calibration, disclosed); D4 truncation audit, any mismatch (40-row sample; coverage = 3 named tape features — its blindness to panel/synthetic features is a *finding*, disclosed per cell); D5 = pooled-significant-but-both-halves-negative (meaningful only with D1); D6 plant s=0.20 recovery < cell null thr = power-break; **D7-recovered = S\* > 0.0113 primary** (the only threshold with wild ground-truth justification — flags the specimen), **D7-oracle = S\* > 0.0169 secondary with the pre-registered prediction that it MISSES the specimen**; D8 pooled |Δuncond| > 1.0pt or |Δbase| > 5pt vs the clean reference; D10 (NEW) monthly composition KS {log_adv20, log_open_px, intensity, pm_to_open} vs pooled H1, threshold = clean-H1 max month-vs-rest KS (1-sample calibration, disclosed). **Measurement = seed-paired continuous margins (S\* − null-q95) alongside every binary call.** Null-sharing rule: row-set-preserving mutants reuse the clean B=200 null (P3_MIX03 runs a fresh null as the registered spot-check); membership-changing mutants get fresh B=60 nulls from the mutant spine. Every membership cell reports the **potency oracle** (cohort-indicator odll); potency ≈ 0 ⇒ the cell is labelled UNINFORMATIVE, never "defended."

**Injector validation criteria (quantitative, frozen):** the SPECIMEN cell must reproduce ≥3 of: single-day share ≥80%; D1 fire with S\* ≥ 0.006 (≥50% of the 13-grid specimen S\*, allowing for the 5-pt grid); zombie cohort base-rate gap ≥ 15pts; potency oracle max ≥ 0.01. Failing this, all P1 capability rows are downgraded to descriptive.

**D9 — the agent layer (sealed protocol; existence-proofs only):** estimand = the *deployed* detector (doctrine-aware via an anecdote-free method checklist; instance-blinded). Sanitized export sandbox (scrub list: doc 273 §4–§7, state files, memory, changelog, git metadata, engine comment lines naming the kill, `*_clean*` artifact names; grep self-check for {zombie, stale, contaminat, clean, 03-23, mutation, gauntlet, 274} before launch). **Auto-demote trigger (pre-committed): if the sandbox cannot be built and audited today, D9 drops to descriptive-only.** Four fleets, sealed assignment (RNG mapping hashed and committed pre-run): 2 clean decoys (mandatory), 1 mutant chosen by the frozen rule (*the lowest-margin FOREIGN cell that all deterministic layers missed; if none, the potent FOREIGN cell with the smallest margin*), 1 real-specimen positive control at full 6-lens structure. Per-referee scoring under the frozen rubric: DETECT = mechanism class + correct locus + survives mechanical adjudication; PARTIAL = class only; decoys score HARD-FP (mechanically-checkable false claim) / SOFT-FP / CLEAN-PASS. **Reporting firewall:** D9 prints in a separate band; rate language pre-banned; permitted claims = existence proofs, the decoy HARD-FP count, and the specimen-replication outcome (a specimen MISS under instance-blinding is a major registered negative: it would mean last night's catch depended on the answer key). P1/P6-family fleet cells are checklist-execution probes (catch uninformative; miss = alarm).

**The confirmatory statistic:** the **surprise count** — cells whose outcome contradicts the frozen prediction column above — with every surprise named and characterized. Banned headlines (pre-committed): any "X% of pathologies caught" aggregate; any per-cell detection *probability* (n=1); any like-for-like ranking of D9 against mechanical layers.

**Outcome meanings (frozen):** **G-COVERED** — no surprises and every potent FOREIGN cell flagged by ≥1 mechanical layer ⇒ the stack's mechanical organs cover the taxonomy; the trust certificate is the margin table. **G-HOLES** — ≥1 potent FOREIGN cell undefended (predicted: P1_ADVHOLE) ⇒ each hole is a named, evidence-backed missing auditor; the stepping stone is the engine's missing organs, chartered. **G-FALSE-CERT** — a DESIGNED defense fails its own target class ⇒ highest-severity regression alarm on the standing rules themselves. **G-UNINFORMATIVE** — potency oracles ≈ 0 across membership cells ⇒ the gauntlet failed to manufacture harm; honest frontier failure, reported with the reason. D9 outcomes report separately per the firewall.

**Known undefended classes & out-of-scope (named, not performed):** vendor PIT revisions (no PIT source exists in the warehouse); symbol churn/ticker reuse (needs identifier cross-reference; Stage 2); corporate-action cross-check defense (the reverse-split arm is first in the Stage-2 queue — the CRCA class); **garden-of-forking-paths / procedure mutation — THE known undefended class** (no detector exists in D1–D10; a cell tonight could only theatrically rediscover what this sentence states for free; chartered for the factory-FDR night); cross-doc data snooping, with the standing disclosure that **the 38 features and the ofi_last10 plant driver were chosen with knowledge of docs 242–273, so even the clean baseline is not snoop-free**; heavy-tail label corruption (structurally defanged by the binary label + winsorization; the single live sub-mechanism — one bad print flipping y via the last-print≥15:00 exit fallback — is disclosed here); row-heterogeneous cost mis-specification (needs doc-270's θ(row); Stage 2). **Stage boundary (frozen):** Stage 1 = validated threat map, audit coverage with margins, existence results, surprise count. Stage 2 (separate prereg) = detection probabilities, ROC/ranking, threshold localization, D9 rates, fleet-level FDR.

---

## §4 — The capability matrix (mechanical layers; run as registered, every deviation declared)

**Declared deviations:** (1) a `day`-column regression crashed the first SPECIMEN attempt (fixed in `_grid_df`, no estimate touched); (2) the registry's importlib-by-file load made the pool worker unpicklable on Windows (the P1/P2 crash class) — import-by-name fix, cells re-run; (3) the **D7 threshold in the committed code (0.040) contradicted the committed prereg (0.0113/0.0169)** — a transcription error caught in-run; D7 is a pure threshold on the stored S\*, recomputed at assembly with the *registered* values (used throughout below); (4) the potency oracle had an index-alignment bug returning 0.0 — recomputed for SPECIMEN where it revealed a deeper truth: **the registered potency oracle is structurally blind to single-day cohorts** (day-blocked CV means the contaminated day never trains the indicator it tests — odll ≈ 0 by construction); cohort potency for concentrated contamination is therefore evidenced descriptively (base-rate gaps, activity ratios), disclosed; (5) a spawn-bomb in a post-hoc patch script (no `__main__` guard) — killed, no artifact damage.

**The matrix** (5-pt grid; margins = S\* − null-q95; registered D7 values; ✓ = matched the frozen prediction, ⚡ = surprise):

| cell | S\* | p | margin | D1 | D2 | D3 | D4 | D5 | D6 | D7rec | D8 | D10 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CLEAN_A | −.0005 | .025 | +.0010 | ⚡FIRE | — | ✓ | ✓ | ⚡FIRE | ✓ | ✓ | ✓ | ⚡FIRE |
| CLEAN_B | −.0005 | .033 | +.0009 | ⚡FIRE | — | ✓ | ✓ | ⚡FIRE | ✓ | ✓ | ✓ | ⚡FIRE |
| SPECIMEN | +.0120 | .016 | +.0070 | ✓FIRE | ✓fresh-FIRE (gate **passes** the zombies!) | ✓FIRE .922@03-23 | ✓ | ✓FIRE | ⚡**FIRE** | ✓FIRE | ✓ | ✓FIRE |
| P1_ADVHOLE | −.0004 | .049 | +.0008 | (base-rate fire) | ⚡overlay-FIRE / ✓truth-FIRE | ✓ | ✓ | (base-rate) | ✓ | ✓ | ✓ | ✓ |
| P1_HOLEREPLAY | +.0079 | .016 | +.0042 | ✓FIRE | ✓overlay-fresh-FIRE (**generalizes**) | ✓FIRE .731@12-08 | ✓ | ✓ | ⚡**FIRE** | silent (<.0113) | ✓ | ✓ |
| P2_ENTRYVIS | +.0160 | .005 | +.0175 | ✓FIRE | — | ✓ | ✓**FIRE** | ✓ | ✓ | ✓FIRE / oracle-✓silent | ✓ | ✓ |
| P3_MIX03 | +.0021 | .016 | +.0039 | ✓FIRE | — | ⚡FIRE (share 2.32 — definition unstable, @2026-04-08) | ✓ (blind, disclosed) | ✓ | ✓ | ⚡**silent** | ✓ | ✓ |
| P3_MIX01 | −.0010 | .030 | +.0005 | ambiguous (= clean false-fire mode) | — | ✓ | ✓ | (base-rate) | ✓ | ✓ | ✓ | (base-rate) |
| P4_SURV | −.0049 | .230 | −.0023 | ✓ | — | ✓ | ✓ | (base-rate) | ✓ | ✓ | ✓**FIRE** | ✓ |
| P5_DRIFT | −.0032 | .131 | −.0016 | ✓ | — | ✓ | ✓ | (base-rate) | ✓ | ✓ | ✓ | ✓**FIRE** |
| P6_NULLFID | — | — | — | tail-deflation +.00074 (asym null anti-conservative ✓); real cross-t coupling **0.68** (the diagnostic separates the constructions ✓) | | | | | | | | |

**THE SURPRISE COUNT (the registered confirmatory statistic): 6 of 11 cells contradicted the frozen prediction at least once; 5 ran exactly to prediction.** ⚠ **The hostile retest (§5b) showed this flat "6" is ill-posed — the honest count is ~4 distinct defect phenomena** (CLEAN_A/B are one byte-identical phenomenon counted twice; two D6 "surprises" are graded against an unregistered baseline, one of them a B=60 coin flip; a falsified P2 mechanism is inconsistently *not* counted). Read §5b before this number. The surprises decompose into four mechanism classes:

1. **The boundary-specificity hole (CLEAN×2: D1/D5/D10 false-fire).** Certified-null data "discovers itself" at p≈0.03 on this grid — 273's p=0.0495 boundary phenomenon reproduced as a *standing specificity defect*, not a one-off: the residual direction-free structure plus the null's n-asymmetry put clean data at its own null's ~97th percentile. D5's conditional definition is degenerate at negative S\* (fires with D1 always), and D10's 1-sample calibration is a hair-trigger (h2max exceeds h1max by epsilon on clean). **Implication: every D1 fire in the matrix is read against this measured false-fire baseline, and the engine's discovery claims at |S\*| < ~0.002 are uninterpretable on this grid.**
2. **Contamination breaks the power instrument (SPECIMEN + P1_HOLEREPLAY: D6 fire ×2, predicted silent ×2).** The planted-signal calibration — the very tool that makes nulls quantitative — is itself a casualty of membership contamination: the contaminated spine's null threshold inflates past the plant's recovery (specimen: recovery +0.0005 vs threshold +0.0051). **A contaminated universe doesn't just fake discoveries; it silently corrupts the instrument that certifies power.** To my knowledge this interaction (the panel's "plant-harness self-contamination," now measured) has no precedent anywhere — it means a null certified with MDE on a contaminated universe is doubly false: wrong about the signal and wrong about its own power to have seen one.
3. **Holes are self-evidencing (P1_ADVHOLE: D2-overlay fired where I predicted silence).** Recomputing the gate over a *holed* warehouse mass-fails verification for hundreds of *clean* neighbors whose windows touch the hole — absence-type corruption cannot quietly agree with the lie, because the absence breaks recomputation broadly. The "warehouse agrees with the lie" geometry survives only for *revision-type* corruption (values changed, nothing missing — the reverse-split/vendor-revision class, out-of-scope tonight). **The frontier of undefended membership pathology is revision-corruption, now with evidence rather than intuition.** Meanwhile the freshness rule generalized to a synthetic hole it was never written for (P1_HOLEREPLAY overlay-mount: 530 fresh-fails) — a genuine capability result, not a regression test.
4. **The implementation gaps measured honestly (P3_MIX03):** the exit-mix dial at λ=0.3 produced +0.0021 nats — far below the predicted D7 regime — so **a moderate label-leak "discovery" passes every designed flag**: D1 calls it significant (p=.016), D4 cannot see synthetic features (disclosed structural blindness), D7's plausibility ceiling sits above it, and the only thing that "caught" it was D3's share-definition instability (share 2.32 > 1 — a divide-by-near-zero artifact landing on the natural anomaly day 2026-04-08). A real leak of this size ships as a finding. **This is the matrix's G-HOLES exhibit: the moderate-leak corridor (+0.001..+0.011 nats) is effectively undefended by the mechanical stack.**

**Designed-organ scoreboard (no G-FALSE-CERT):** every defense caught its design class — D4×P2 (look-ahead at the tape level), D2b×holes (both wild and synthetic, both mounts), D8×survivorship (base-rate band at 2 of 5 t), D10×drift (h2max 0.241 vs 0.112), D7-oracle correctly *missed* the specimen exactly as pre-registered, and the injector validation criteria passed (D3 share .922 ≥ .80; S\* ≥ .006; base-rate gap ≈21pts) — the in-harness specimen reproduces the wild kill's signature.

## §5 — D9: the agent layer, measured under seal (existence proofs per the firewall; rate language banned)

**Protocol as registered, with two integrity notes:** the sandbox grep self-check FAILED on first build (the sanitizer leaked `clean`-suffixed CLI/prints and the verdict narrative; the audit also flagged two vocabulary collisions — "permutation"⊃"mutation" and `stale_at_t`, a registered feature name whose renaming would itself create spec divergence) — sanitizer fixed (core-engine truncation, word-bounded tells), rebuilt, **PASS**; the auto-demote trigger ("cannot be built AND audited today") was read as iterate-to-green, with this disclosure standing for the referee fleet to rule on. Assignment sealed pre-launch (sha256 `8267c2a9…` in the state file; 2 decoys + 1 frozen-rule mutant + the specimen). The frozen mutant rule landed mechanically on **P3_MIX03**. Blinding was instruction-level, not filesystem-level (disclosed); and **ASSIGNMENT.json sat inside the sandbox root — a layout flaw**: R1's *synthesizer* (not its lens agents) disclosed incidentally seeing the run labels while locating the warehouse; its verdict rests on lens evidence gathered before synthesis, and the synthesis-layer blinding for R1 is downgraded accordingly.

**The four fleet verdicts (24 blinded lens agents + 4 synthesizers; every defect claim survived adjudication):**

- **R4 = the mutant — DETECT, the existence proof in its strongest possible form.** Three lenses independently reverse-engineered the injection to near-exact construction ("≈ gross_ret + unit Gaussian noise, z-scaled"; the actual injection: λ·z(exit/last-prefix) + √(1−λ²)·noise at λ=0.3), proved *usage* by reproducing all ten reported I values **to <1e-12** with the hidden column and a sign flip without it ("the shipped code cannot reproduce the shipped result — the column was both used and hidden from code review"), and proved *post-t content* by the future-leg correlation decaying exactly as remaining-future shrinks. **Under instance-blinding, the agent layer caught precisely the pathology that sits in the mechanical stack's undefended corridor (§4.4).**
- **R2 = the specimen — DETECT: the doc-273 kill replicates without the answer key.** The blinded fleet re-derived the whole mechanism from scratch: the export hole, the 82-day-stale prev close, 389 mass-admitted tickers of which **6/389 truly gapped ≥8%** (ground-truth tape recomputation), the dead-tape cohort fingerprint, 92% concentration, LODO collapse, both split-halves negative — and explicitly cleared the pipeline itself ("950+ rows rebuilt bit-exact; the defect is upstream data integrity"). The single most important D9 result: **last night's catch was not retrieval.**
- **R1, R3 = the "decoys" — both returned DEFECT, and both are RIGHT.** The decoy condition failed by construction: the doc-273 "certified-clean" universe is itself latently contaminated — the lag≤4d freshness filter caught stale *prev-closes* but not stale *ADV windows*: every post-2026-03-24 qualifier's trailing-20-ROW window bridges the Jan–Mar warehouse hole into December 2025. **Adjudicated by my own recomputation: GLXG 2026-03-24 stored ADV20 = $1,083,888; true trailing-20-session tape ADV = $37,445 (28.9×; true gate FAIL).** 669 ticker-days on 24 post-hole sessions carry bridging windows; the fleets' stratified sampling estimates ~51 true gate-flippers (~255 rows), including most of the 2026-04-08 cohort's windows. Consequently **the registered decoy/false-positive measurement is VOID — there was no clean substrate to measure a false-fire rate on** — and what stands in its place is better: zero fabricated claims across 24 agents (every claim survives recomputation), and the gauntlet's own control cells discovered a real, previously unknown defect in the baseline, exactly as 273's experiment discovered its universe. The chip is updated (`task_4994579a`, superseding `task_80964988`): the rebuild now requires *window-integrity*, not just prev-close freshness. Doc-273 impact: direction-preserving (removing illiquid zombies strengthens a null; A(t)<0 unaffected a fortiori) — but "certified-clean" carries an asterisk until the rebuild.

**What D9 may claim under the firewall:** (i) an existence proof — under instance-blinding the deployed-protocol fleet detects a moderate-strength label-leak that the mechanical battery's designed flags missed, with mechanism+locus+reproduction; (ii) a blind replication — the wild 2026-03-23 catch reproduces without doc-273 knowledge; (iii) a discovery — the control substrate itself was defective and the fleets found it. **No detection rates, no rankings** (n=1 per condition; the FP rate is unmeasured and now unmeasurable until a truly clean substrate exists).

## §5b — The mandated hostile retest turns on the gauntlet itself

A 7-agent fleet (`wf_8f296538-7e3`: six referees with distinct lenses + a completeness critic) attacked §4/§5 with full read access, each instructed to refute. **Every ruling came back QUALIFIED, high-confidence** — the substantive findings survive recomputation, but the *framing* of the confirmatory statistic does not. In the spirit of 273's §4→§6 distance, the corrections are the finding: **a gauntlet built to measure the engine's blind spots had blind spots in its own instrumentation, exposed only by turning the same adversarial machinery on itself.**

**What survives airtight (the genuine deliverable, independently re-verified):** the R4 existence proof. The shipped 38-feature engine *cannot reproduce its own shipped statistic* — running it over R4's spine yields S\* = −0.00048 (clean), while the reported +0.002085 reproduces bit-exact only when the hidden `factor_resid` column is added (corr 0.305 with the label = exactly the λ=0.3 leak). The seal is honest (ASSIGNMENT sha256 `8267c2a9…` matches the pre-launch record; prereg + cell + mutant rule committed at `46fd6be` *before any cell ran*, git-verified). So: **a sealed, pre-committed, blinded label-leak passed the entire mechanical battery** — D1 called it significant, D4 is structurally blind to synthetic columns, D7's ceiling sits above it, and D3's lone "catch" is an artifact (below). That is an evidence-backed, reproducible fact about the doc-273-class engine, not an intuition. The latent ADV-window defect is likewise adjudicated as a **true positive** (GLXG and ≥4 more independent gate-flippers at 20–31×).

**The corrections that bind (all adopted):**

1. **The "6 of 11 surprises" headline is ill-posed; the honest count is ~4 distinct defect phenomena.** The confirmatory statistic the whole prereg was built to deliver cleanly does not survive a consistent application of its own definition: (a) **CLEAN_A and CLEAN_B are byte-identical engine outputs** (same I/S\*/D5/D10; only the null is re-rolled) — one phenomenon counted twice; (b) the two D6 "surprises" (SPECIMEN, P1_HOLEREPLAY) are graded against an **implied "predicted silent" baseline that is not in the frozen §3 rows**; (c) **P1_HOLEREPLAY's D6 fires by margin −1.3e−5** at reps=3/B=60 — a coin flip; (d) meanwhile **P2_ENTRYVIS's registered *mechanism*** ("both halves positive") **is falsified** (H2 = −0.0253) yet is *not* counted — the opposite convention to the lenient D6 inclusion. Deduplicate the clean cells, drop the coin-flip, apply one convention, and the count is **4 distinct defects**: clean self-discovery at |S\*|<0.002; ADV-hole self-evidencing via D2; the moderate-leak corridor passing every purpose-built flag; the SPECIMEN power-instrument break. The flat "6" is a cell-count dressed as a defect-count.

2. **G-HOLES is the right outcome *family*, but the registered FOREIGN-cell trigger never fired as frozen.** The prereg pre-committed G-HOLES to "≥1 potent FOREIGN cell undefended (predicted: P1_ADVHOLE)." Outcome: **P1_ADVHOLE returned potency oracle = 0 → UNINFORMATIVE by the prereg's own rule**, and D2-overlay *self-evidenced* (gate_fail=687); the other potent FOREIGN cell, P1_HOLEREPLAY, was **defended** (D2b overlay fresh_fail=530). Neither potent membership cell is undefended. The operative undefended hole is instead the **moderate label-leak corridor** (P3, a *non*-FOREIGN class) — a post-hoc trigger substitution that must be disclosed as such. The membership FOREIGN class yielded surprise #3 (absence-type holes self-evidence), leaving **revision-corruption** (values changed, nothing missing — the reverse-split/vendor class, out of scope) as the only residual undefended *membership* sub-class.

3. **D3 is mis-implemented versus its own prereg** — and it was the lone mechanical "catch" of the leak corridor. The registered rule was "share >50% **AND** total mass > the clean-cells' max (3-point calibration)"; the shipped `d3_lodo` fires on `share>0.50` **alone** — the second conjunct and the calibration were never built, and the clean-cells' total mass is *negative* (−1.35), making the anchor degenerate. So D3's P3_MIX03 "fire" (share 2.32 = 13.87 nats / 5.97 net total at the natural-anomaly day 2026-04-08) is a **divide-by-near-zero artifact, not a catch** — which *strengthens* the §4.4 conclusion: the moderate-leak corridor is undefended by *any* purpose-built organ.

4. **"Contamination breaks the power instrument" — confirmed once firmly, mechanism reframed.** The D6 fire is the contaminated null's **threshold rising** (+0.0065) past a recovery that itself slightly *rose* (+0.00045) — *not* "the plant no longer recovered." The cell can no longer distinguish a real s=0.20 signal from its own contaminated null. Firm at SPECIMEN; borderline at P1_HOLEREPLAY. The **CLEAN_B fresh-null control** (thr95 −0.00142 ≈ CLEAN_A's shared −0.00149) refutes the "fresh-null-per-cell artifact" alternative — inflation tracks *contamination content*, not the construction.

5. **D9, narrowed honestly.** The method checklist shipped to the blinded fleets literally contained the factor_resid-finding recipe ("correlate candidate columns against exit/last-prefix statistics") and the ADV-bridge check — *in-spec* for the registered "doctrine-aware deployed detector" estimand, but it means the R4 catch is **"a doctrine-aware checklist-executor found the planted column," not LLM-specific frontier-extension**; a trivial deterministic reproduction-from-stated-features check catches the identical leak with a sign flip. Two further corrections: the **latent ADV defect was in all four D9 spines, but only the two decoys reported it** (R2/R4 missed it) → fleet-level latent-defect sensitivity ≈ **0.5**, which must temper the "immune system working" reading; and **"zero HARD-FP across 24 agents" is asserted, not auditable** (the lens-agent transcripts were not persisted to disk — only `engine_result.json` and the spine columns are).

6. **No G-FALSE-CERT — correct.** Every designed organ caught its own class (D4×P2 40/40; D2b×holes both mounts; D8×P4 at 2/5 t; D10×P5 h2max .241; D7-oracle correctly *missed* the specimen as pre-registered). D7-recovered fired on its target (SPECIMEN); its miss of the never-targeted P1_HOLEREPLAY is a *capability hole*, not a designed-defense failure. And the doc-273 impact (`task_4994579a`) is **empirically direction-preserving on the specific correction** — softened from "a fortiori," because dropping the whole 2026-04-08 cohort over-corrects A(t) positive at 3 t (a corpus-perturbation artifact, not a real edge).

## §6 — Final verdict and implications

**VERDICT: G-HOLES (outcome family), with the registered FOREIGN-cell trigger disclosed as not-fired.** The falsification engine's mechanical organs each caught their own designed pathology class and there is **no G-FALSE-CERT** — but the gauntlet located **two real, evidence-backed undefended holes**, one in the engine and one in the gauntlet's own instrumentation:

- **The engine's hole — the moderate label-leak corridor (≈ +0.001 to +0.011 nats).** A label-leak of that strength (exhibited at λ=0.3 → +0.0021 nats, sealed and blinded) is called *significant* by the permutation null, is *invisible* to the look-ahead audit (which only sees tape features, not synthetic/panel columns), sits *below* the plausibility ceiling, and is *not* caught by any purpose-built organ (D3's apparent catch is an artifact). The one thing that exposes it is a **reproduce-the-result-from-the-stated-features check** — which the engine's battery does not contain. That is the single most actionable finding: the doc-273-class engine, as built, will certify a moderate label-leak as a discovery.
- **The gauntlet's hole — its own confirmatory statistic.** The "surprise count" was ill-posed (cell-count vs defect-count; inconsistent inclusion conventions; a mis-implemented D3 organ that contradicts the prereg). The hostile retest caught all of it. **The lesson is recursive and is the night's real stepping stone:** the discipline stack cannot certify *its own* instrumentation any more than it could certify universe membership (273) — and the only thing that caught either was turning the adversarial machinery inward. 273 found a blind class in the *inputs*; 274 found one in the *measurement apparatus itself*.

**What this stepping stone enables (the deliverables that ship):**
1. **The reproduce-from-stated-features auditor** is now specified by an existence proof — the cheapest, highest-value organ the engine is missing. Chartered as the first build of the Stage-2 immune system: every certified result must re-derive its own statistic from exactly its documented feature list, or it does not ship.
2. **The membership immune system is live** — `src/research/experiments/engine_immune_system.py` registered on the doc-272 substrate, re-auditing the shared research corpus against the raw warehouse every night (gate + freshness + ADV-window-completeness). First run: **1,486 of 10,786 corpus ticker-days flagged (13.8%)** — the standing red light is on, and stays on until `task_4994579a` rebuilds the day_aggs hole and the window-integrity guard lands.
3. **The corpus is known-contaminated, corpus-wide.** Beyond the doc-273 window, the immune system shows the ADV-window-bridging cohort touches the full 2024–2026 corpus that docs 235–273 all consumed. Direction-preserving for the *nulls* (the violators are illiquid zombies), but any future *positive* on this corpus must clear the membership audit first — now mechanically enforced.

**The next hard question, sharpened by tonight:** the factory-FDR night (273 candidate D) is now the natural successor — not "how many spurious edges does N experiments admit," but "build the reproduce-from-features + membership + fleet-deflation triad as the substrate's *gate*, and validate it against a certified-null synthetic fleet that includes planted leaks of graded strength." Tonight proved the gate is needed and specified two of its three organs; that night builds and certifies it.

**Not one number bent — including, this time, our own.** The gauntlet measured the engine honestly, then the retest measured the gauntlet and found its headline statistic wanting; both are on the record at full strength. The engine that audits everything except itself is exactly the engine 273 warned about — and 274 turned that warning into a named, buildable organ.
