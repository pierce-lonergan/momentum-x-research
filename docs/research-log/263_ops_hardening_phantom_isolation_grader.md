# 263 — Ops hardening: phantom-ghost root-cause fix, bot account isolation, same-day grader

**Author**: Claude Opus 4.8
**Date**: 2026-06-08
**Mandate**: Pierce — fix the latent ops hazards from doc 262 (shared account, phantom-ghosts, shadow lag) and wire up automatic same-day shadow-vs-prod grading. Touches the live close path — applied surgically and tested.

## 1. Phantom-ghost root cause → FIXED at the source (main.py D163)
Two agents traced today's 3 ghosts (TNGX/ABAT/OCC). Verdict: **CLOSE-SIDE phantoms**, created by the **D163 software trailing-stop** path (`main.py:6064-6088`). That path called `client.close_position()`, swallowed the 403 that fires when the qty is `held_for_orders` by the OTO child stop, fell back to a market order that also failed, then **booked realized P&L from an MTM snapshot price** (not a confirmed fill) and cancelled the protective stop — leaving the position naked until the EOD force-close. TNGX booked a pure phantom **+$746.82** (broker $0); ABAT booked **+$730** (broker $528). This poisons the journal/BOCPD/Kelly corpus; only `broker_truth_recon` (−$1,172.71) is trustworthy.
**Fix**: route the D163 close through `attempt_close_with_status_check(..., cancel_blocking_stops_first=True)` and add the **Bug-Z gate** — *if the broker close did not succeed, do NOT cancel stops and do NOT book P&L; leave the position tracked + protected and `continue`*. This is the exact hardening the **D78 SMART_EXIT** path (`main.py:6280-6326`) already had and that D163 was missing. Only a **confirmed broker fill** books P&L now. **34/34 close-path tests pass** (`test_bridge_state_machine`, `test_wiring`, `test_invariant_injection`).
**Follow-up (flagged, not yet changed)**: the sibling price-based partial-exit paths **D164 EARLY_PROFIT_TAKE** (`main.py:~4393, ~5720`) and **D165 tranche-take** share the same submit-then-book-from-computed-price pattern. They did not fire today's ghosts (today's were TRAILING_STOP) but carry the same latent phantom for any 403. They should get the same gate in a follow-up (they're partial-sell paths, a slightly different shape — deferred to avoid sprawling the live hot-path edit in one pass).
**Secondary (flagged)**: OCC's journal/broker disagreement (+936 delta) is a *different* bug — `trade_journal.record_close()` ticker-fallback (`trade_journal.py:586-595`) re-stamps an intraday re-buy's close onto the *first* BUY's trade_id, clobbering the prior round-trip's P&L. Fix is to target the still-*open* same-ticker BUY. Deferred (journaling-only, doesn't create a broker ghost).

## 2. Bot account isolation → plumbing shipped + immediate shared-account cap
All three bots (prod, Lottery, FaderShort) read the same global `ALPACA_API_KEY` → one shared paper account `PA3A2I4TN9AZ`. Risk: Lottery sizes **Aggressive-Kelly up to 50% of total equity** (`LOTTERY_BANKROLL_PCT=1.0`), so a fired ELITE pick could draw on prod's cash.
**Shipped** (`scripts/lottery_runner.py`, `scripts/fader_short_runner.py`):
- Per-bot key override: each bot now prefers `LOTTERY_ALPACA_*` / `FADER_ALPACA_*`, falling back to the shared `ALPACA_*` if unset (so **no behavior change today** — safe).
- **Immediate mitigation while shared**: Lottery's bankroll is **hard-capped to $5,000** (`LOTTERY_SHARED_ACCOUNT_CAP_USD`) whenever it is NOT isolated — so even if the meta-scorer fires, it cannot draw ~$108k of prod equity. FaderShort is already bounded (~$1,250 fixed notional).
- Loud startup log: each bot now logs `ISOLATED` vs `SHARED with prod (capped)`.
- **ACTION REQUIRED (Pierce)**: create two Alpaca **paper** sub-accounts and add to `~/momentum-x-secrets.env`: `LOTTERY_ALPACA_API_KEY` / `LOTTERY_ALPACA_SECRET_KEY` and `FADER_ALPACA_API_KEY` / `FADER_ALPACA_SECRET_KEY`. The runners pick them up automatically (launchers parse secrets → process env) and the cap lifts. Until then, the bots are safe (capped) on the shared account.

## 3. DataIngest lag → grading made lag-immune (no DataIngest change needed)
The lag is **inherent**: DataIngest pulls Polygon **S3 flat files**, which for *today* aren't published until ~04:00 ET *tomorrow* (`daily_data_ingest.ps1:279-283`). So the 19:00 rocket/red-flag shadows legitimately can't score today's gappers. This only affects *research freshness* — prod trades off the live API, not the lake. Rather than re-plumb DataIngest, the **grader fetches same-day prod-trade outcomes directly from the Polygon REST open-close endpoint** (available right after the close), so **prod grading is same-day** regardless of the warehouse. The red-flag/rocket cross-reference backfills automatically on the next day's grader run once the lake catches up.

## 4. Same-day shadow-vs-prod grader → built + scheduled (the "wire up" ask)
`scripts/shadow_vs_prod_grader.py` (+ `run_shadow_grader.ps1`, scheduled **MomentumX-ShadowGrader @ 19:30 ET** daily). Each evening it: parses prod's trades (journal) + EOD broker truth/ghosts, parses Lottery (meta tiers/SKIPs) + FaderShort (short attempts/not-shortable) logs, reads the red-flag + rocket shadow logs, **fetches same-day open→close per ticker via REST**, then cross-references prod's longs against every shadow signal and grades whether a shadow veto would have helped or hurt — and posts the scorecard to OPS_ALERT.
**First run (2026-06-08) — and it corrects doc 262**: prod actually opened **19 longs** today (doc 262's manual pass under-counted at 1), a **high-variance** book — winners NPT **+274%**, BYAH +121%, MTEN +76%, TDIC +60% alongside losers RMSG **−45%**, BGMS −34%, GMHS −33%, DEVS −29%. The Lottery meta-scorer flagged 2 of prod's longs (RMSG SKIP@0.15, GMHS SKIP@0.17) — **both lost** (−45% / −33%). Highest-MFCS name OCC (0.62) went −3.1% (MFCS still anti-selective). The automated grader now accumulates this prod-vs-shadow scorecard nightly toward power.

## Honest status
The phantom-ghost **source** is fixed for the path that fired today (broker-confirmed close only); two sibling paths + the OCC journal-overwrite are precisely flagged for a careful follow-up. Account isolation is **plumbed + capped** (full isolation pending Pierce's two paper sub-accounts). The shadow-vs-prod comparison now **runs automatically every evening**, same-day for prod via REST. Net: the live system is materially safer and self-grading. **The phantom fix is the one change that touches real P&L booking — it strictly *prevents* booking on unconfirmed closes, never the reverse.**
**Basis**: `main.py` (D163 close-path), `scripts/lottery_runner.py`, `scripts/fader_short_runner.py`, `scripts/shadow_vs_prod_grader.py`, `scripts/run_shadow_grader.ps1`, MomentumX-ShadowGrader task. **Predecessor**: 262 (the forensic that surfaced these). **Memory**: PHANTOM-P&L bug class.
