# 270 — Live-fire proof, the realism layer, and the cost of everything (the day's record)

**Author**: Claude (Fable 5 era) | **Date**: 2026-06-10 | Companion: doc 271 (the Huayra blueprint). Deployed tree at session start: c9c36b7 (08:01 restart). HEAD at close: **cb880fd** — deploys Thu 04:30.

## A — LIVE-FIRE VERDICT (the new machinery against real tape)
- **VLL day-1: flawless.** 93 events, every verdict terminal'd: BLOCKED_CATALYST_GATE 76 (8 tickers), BLOCKED_NEWS_GATE 14 (8), SUBMITTED 3 — exactly matching the 3 broker fills. Never-raises held. Coverage gap on record: **3 of ~26 gates emit** (D170/cap/spread/etc. still silent — the emit-map completion is the next DORMANT-C pass).
- **The audit earned its existence on day 1: a NEW phantom sibling caught with two same-day specimens.** HWH: D165's T1 market sell (5,479 @ $2.02) hit the websocket → the **D297 handler booked it as the TERMINAL close of all 16,437 shares** (STOP_FILL @ the tranche price, +$54.79) and evicted the position; the real stop's −$970.94 wave (a dropped `partial_fill` + a silently-ignored `fill`) was never booked. FLD identical (−$68.55). EPSM: same chain + **5.8-hour broker ghost with 700 shadow-LETHAL ticks into an unread log**. Journal-vs-broker on the day: **+$1,039.49 of phantom**, while the hardened doc-269 paths (D164/D165 booking side) behaved exactly as designed (no booking without confirmation — D165 refused; D297 betrayed it downstream).
- **FIXED — `cb880fd` [LIVE-A]**: partial-aware per-leg booking in `fill_stream_bridge` (order-cumulative increment math, execution_id dedupe before side-effects, tranche-ownership + short guards, broker `position_qty` as terminal truth, per-leg journal rows via doc-269 semantics). Gauntlet: **138/138** consolidated gate including the exact HWH replay; hostile line review passed. Residuals declared (D164 double-journal + full-close double-row patterns pre-existing; PM sink ownership unchanged).
- **The proof the cutover thesis wanted**: tonight's three-way recon, end-to-end through its real consumer: **LEDGER $−714.68 = BROKER $−714.68 (drift $0.00)** on the very day the journal drifted +$1,039. Three exact-zero ledger days (6/3, 6/9, 6/10). Adversary on the deployed tree: wrapper_bugs **[]**, 10/11 (the known EOD-carry flag only). Residual gauntlet of the 08:01 early deploy: recorded complete.

## B/C/D — SHIPPED TODAY (commits, risk-tagged)
`269c032` A3 test debt retired (28/28 + 248/248; full baseline **4180 passed / 76 failed**, buckets published, headline failure attributed pre-existing) · `bb73e5d` C2 **ledger shadow live** (avg-entry fold; shadow clock day 1 = tonight) · `2fdf9b4` D1 **cost census** (dead-fallback bleed ~89 calls/day ENDED with today's boot — env verified pin-free; EDGAR 224 calls/5d at 100% failure; **Finnhub ALIVE** — doc-249 drop-assumption corrected; tier2 ensemble p95 23.4s vs 25s timeout = staged report, no tuning) · `08a3dad` D2 **EDGAR hardened** [LIVE-A] (bounded 10s, retry-on-5xx-only, 12h/30min cache, never-blocks; 29/29) · `1cfab0f` pre-19:30 fixes (grader `import sys` — caught by the fleet assessment before it fired; **watchdog weekend guard** — Saturday ~200-post storm defused) · `cb880fd` D297 fix [LIVE-A]. **B1 fidelity matrix published** (agent-measured, spot-verified): every submitted order filled 13/13 → **100% of sim divergence = pre-submission gates** (D200 56% / D204 31%); all sims C−/D as fill models; `execution_model.py` unification spec captured. `simulate_day.py` confirmed never-existed (stale lore corrected).

## FLEET SCORECARD (assessment agent, evidence-verified; no A's — honest)
PaperTrading **B−** (D297 leak → fixed tonight) · Lottery **B+** (shared acct) · FaderShort **C+** (0/4 shortable again; retire/repurpose decision pending) · Watchdog **B−** (weekend guard → fixed; alert path still never test-fired) · DataIngest **B** (f-string fix verified working; **stop_widening_replayer dead since 6/4**, parquet frozen 5/25 — chipped) · RocketShadow **B−** (fwd n=154: 0% hit, top-1 −26.9% — kill-criterion proposed) · RocketWatchlist **B** (130 LLM rows accumulating; report verdict-line fix chipped) · ShadowGrader **B+** (sys bug → fixed) · health_check **B+** (registry self-verification chipped) · ledger_shadow **B** (day-1 restored by the sys fix) · VLL **B−** (coverage 3/26) · usage_audit **C+** (schedule it) · incident_bus **C** (13 synthetic CRITICALs, 0 real consumers — **the priority gap**: today's real 5.8h naked position emitted nothing) · recon_daemon **B** (caught EPSM in 7s, told no one ×700) · fill_capture **B+** (completeness gap: EPSM's 16:01 force-close fill missed — websocket ends at the bell; nightly count-vs-broker assert chipped). **Systemic top-5**: alarms-without-sirens; book-from-intent (now fixed at the last known site); shared-account/synthetic entanglement; never-test-fired alerts; untested producer-consumer contracts (caught me too — the grader sys bug). **Leverage top-5**: one escalation spine; one booking library (= ledger cutover); isolation + synthetic namespace; registry self-verification + content-dated freshness; self-testing alert drills.

## PIERCE'S CHECKLIST
1. Standing flip lines (T2 0.75 / cap 16 / size 5%) + isolation keys (Lottery still $5k-capped).
2. **D200/D204 dollar brief**: day-1 VLL counted 76+14 blocks; the nightly grader now prices each blocked name — the EMPTY≠BEAR lever (G2) builds next, flag-OFF, decision yours.
3. **Real-time CRITICAL paging arm** (G1): today's EPSM proved the cost — 5.8 hours naked, 700 unread warnings. The drainer builds flag-OFF next session; arming is yours.
4. Rot dispositions: RocketShadow kill-criterion, FaderShort repurpose, selection_arena/inverted_shadow/drift_cron pruning — one-pagers staged.
5. Thursday 04:30 deploys: `08a3dad` (EDGAR) + `cb880fd` (D297) — both gauntleted; rollbacks one line each.

**Chip net**: closed 4 (testdebt ×2, grader-sys, weekend-guard) + fixed-at-source 2 (EDGAR, D297); added 10 (all evidence-tagged). Register larger but radically better-lit. **Not one number bent.**
