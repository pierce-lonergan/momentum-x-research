# 285 — THE FIRST-SESSION GAP HUNT: what "+0.686%" actually was, and the twelve gaps under it

**Author**: Claude (Fable 5, ultracode; 4 gap-hunters + synthesis, wf_2cf433f8, live mid-session read-only) | **Date**: 2026-07-06 | **Class**: LIVE FORENSIC + STAGED CONFIG (one corrected env package applied for tomorrow; no live-process change) | **Trigger (Pierce)**: "It genuinely looks like strong results… 0.50%+… Where are the gaps?"

> The honest headline, verified to the dollar: **+0.686% was never trading profit.** It was two unrealized marks — 79% of it LUCY, a D106-flagged 80%-probability promo pump at 4,755× RVOL — sitting on a realized **−$940** day (−$1,007 weekend-orphan gap-down + one real +$67 RIVN tranche clip). It decayed to **+$174 by 11:52**. Worse: since a 10:48 connectivity blip, **the bot believes it is flat** — a circuit-breaker-open `get_positions()` returned "empty," and the D98 stop-out poller booked **two phantom stop-outs** on live positions. $35.7K of stock (both currently green) is running on static broker stops with no active management. The session also *proved* the doc-282 sizing package was a **dead letter** (executor uses D150 tier caps + Kelly-tier risk, not the EXEC_ knobs) — RIVN sized at 15.5% of equity on 2% risk, 3× the intended posture. **Today is an ordinary selection day at 3× intended size whose green is one promo-pump mark — saved twice by shadow-mode discipline and once by a backstop stop.**

## §1 — P&L attribution (reconciles exactly)

Day(t) = **−$1,007.10** orphan flatten vs Friday marks (PPCB −$254, TC −$504, YRD −$249 — the weekend gap-down doc-280 predicted) **+ $67.21** RIVN T1 clip (real, broker-confirmed) **+ unrealized(t)** (snapshot +$2,260: LUCY +$1,794 = 136% of the day's gain, RIVN +$466). Day range so far: −$908 (10:50) → +$1,320 (11:48) → +$174 (11:52). Selection cadence was normal (2 entries by noon ≈ clean-book rate); **size was 2–4× the clean book**.

## §2 — The gap ledger (twelve, ranked)

1. **[CRITICAL/operator, before 16:00] $35.7K unmanaged live stock.** The 10:48 phantom-close made RIVN + LUCY broker-side ghosts: no trail, no smart exit, no EOD ownership — only GTC stops (LUCY $0.9843, RIVN $16.41, verified resting) + whatever the EOD ghost-cleanup does. **Decision: re-adopt (restart intraday) or manually flatten before the close, or consciously ride the stops + EOD failsafe.**
2. **[CRITICAL/agent-flag-gated] The PHANTOM-P&L class recurred, inverted**: D98 treated a circuit-breaker-open `get_positions()` failure ("using empty") as *positions gone* → booked stop-outs that never happened (−$1,272 phantom realized; regime flipped normal→cautious off a fake loss). Fix class: broker-absence must be CONFIRMED (fresh successful fetch + stop-order fill status), never inferred from a failed call. Same family as `attempt_close_with_status_check`.
3. **[CRITICAL — CORRECTED TODAY] The doc-282 sizing knobs were dead letters.** `EXEC_MAX_POSITION_PCT` is read once then overwritten by the D150 aggressive-mode tier pcts (50/30/15%!); `EXEC_RISK_PER_TRADE_PCT` is shadowed by the D115 Kelly tier (2%; tiers 2–4 default 4/6/8%!). RIVN reconstructed to the share: $3,831 risk (2%) → tier3 15% cap → 1,552sh, filled +4.4% above the cap's anchor = 15.65%. **Fix applied to the operator env for tomorrow's launch** (backup `.bak-doc285`): `EXEC_TIER1/2/3_POSITION_PCT=0.05`, `KELLY_TIER1..4_RISK_PCT=0.0067` — verified through Settings.
4. **[CRITICAL/operator] `MOMENTUM_EXIT_POLICY=t1_next_open` is live from a Windows USER-scope env var** — invisible to file greps, contradicting doc-280's verdict (overnight = the one robustly negative arm). **Staged one-liner:** `[Environment]::SetEnvironmentVariable('MOMENTUM_EXIT_POLICY',$null,'User')` (rollback: set it back). Also explains the Thu→Fri carry that created the weekend orphans.
5. **[CRITICAL/operator-or-chip] Tier-1 LLM dead all session, zero alarms** — Together rejects `Qwen/Qwen3.5-397B-A17B` as non-serverless on every call (≥21 provider errors); the bot traded on a degraded agent stack silently.
6. **[CRITICAL/agent-tonight] The meta-gap: no config-truth recon.** Doc-282 verified env→Settings binding offline; nothing verifies Settings→*hot-path behavior*. The sentinel: nightly, compare each fill's notional/equity and risk-at-stop against intent; alarm on breach (the "broker_truth_recon for config").
7. **[MAJOR] Tonight's ledger is fiction in both directions** — internal −$1,204 carries −$1,272 of phantom stop-outs; `trade_results.jsonl` needs the two phantom rows tagged `infrastructure_contaminated` (the LFS/APPS pattern) when the real closes book.
8. **[MAJOR] Re-entry ban + D277 halt have a structural bypass** — both live only in `submit_oto_order`/`submit_oto_short_order`; any close/emergency path that buys or non-OTO entry path skips them.
9. **[MAJOR] Stale-anchor class**: caps/stops/targets computed on the frozen eval price while D190 chases the live ask — RIVN's tranche geometry collapsed (T1 just 0.7% above fill → 4-minute clip) and a 5% cap can fill ~5.4%.
10. **[MAJOR] The position book lost its own positions in stages**: the tranche partial-sell never decremented internal qty (Bug-D regression), stop order-ids were unregistered internally ("⚠UNVERIFIED"), then the phantom close wiped both.
11. **[MAJOR] Three subsystems hold three different views of the protective stop** (executor OTO leg vs D313 watcher vs eod_recon) — the D313 backstop worked but burned 41 futile 403s first.
12. **[MAJOR/agent-tonight] Instrument dark-running is still unmonitored** (the collector-dark-4-weeks class): the rocket-gate ledger + Kalshi shadow need fail-loud checks on their first real nights (tonight).

## §3 — What is genuinely good (not stolen by the audit)

T2 75/25 live and correct (sticky per-ticker, 6/9 wide; **it halved LUCY** — without it she'd be 15% of equity); the RIVN T1 clip executed exactly as designed; **zero stop≥fill submissions** (the doc-282 class did not recur); the holiday-orphan fix handled the morning flatten 403s correctly; the D313 watcher backstopped RIVN with a real emergency stop when the OTO leg failed; **shadow-mode discipline paid twice** (RECON_LETHAL in shadow would have force-flattened two winning positions on the broken internal read — and didn't); selection cadence stayed honest; elite press off, fader dry, max_positions=8 all verified live.

## §4 — Bottom line

Not fantastic, not a disaster: **an ordinary selection day at 3× intended size, its green one promo mark, its books corrupted by a phantom close, its guardrails half-landed** — and every one of those four clauses now has either a fix applied (sizing), a staged one-liner (exit policy), a chip (phantom-close class, LLM model), or a sentinel design (config-truth recon). The doc-283/284 instruments run their first real night tonight. The posture that was *supposed* to trade today arrives tomorrow at 4:30 AM.
