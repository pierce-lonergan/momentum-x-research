# 281 — WHERE PROFIT LIVES: the full-system answer to "how do we make this thing profitable"

**Author**: Claude (Fable 5 driving, ultracode; 6 grounded investigations × 3 adversarial judges × synthesis, ~816K subagent tokens, every claim repo-verified) | **Date**: 2026-07-04 | **Class**: DORMANT-C (read-only vs production; every live change is an operator env line, staged for Pierce) | **Question**: the direct one — *how can we make this thing profitable?*

> The apparatus was pointed at the whole system with one question and no protected priors. The answer it returns is the one the seven-night arc has been converging on, now stated with tape-verified numbers: **there is no offense to ship.** Every alpha door is closed (selection, 245–260), retracted (the +9.47% fade-short pilot → −1.65% on the corrected n=66), OOS-failed (the RVOL-gated hold), or physically unfillable (the short side: **0-for-94 live borrow attempts**). The only gauntlet-grade dollars found anywhere are *negative* facts — the broker-truth bleed, a **log-verified vanished-stop bug class on the book's worst trades**, and the borrow wall. This week's honest profit lever is **losing slower** — cut size, honor the one pre-registered rule that actually fired (T2 wide), and run the stop forensic that decides whether the risk system was ever protecting the book — while a $0-credited measurement layer earns back the right to play offense. And the quiet part out loud: **"trade nothing" beats every package** (+$43–71K/60 sessions vs status quo); the residual bleed is the price of keeping the experiment and its instruments alive, not a strategy.

## §1 — Method

Six hypotheses — every door the falsification ledger leaves open — each investigated by a grounded agent against the actual repo (docs, configs, logs, journals, tape, live broker artifacts), then scored by three adversarial judges (evidence-rigor / deployability-risk / expected-dollars), then synthesized. Key facts were independently re-verified by the driver (the 0-for-94 log accounting, the shadow scorecard, doc 251's cells, the session P&L distribution).

**Ground truth established first:** the bleed is *broad, not tail* — median session **−$358**, only 7/32 positive, clean book −$22,961 (−$718/session); broker-equity truth is worse (**−$1,185/session**; the book under-attributes — recon shows sign-flips and 4 book-absent losing sessions). Any profit story must beat that baseline honestly.

## §2 — The graded slate (three judges, unanimous ordering)

| # | Grade | Hypothesis | Verdict in one line |
|---|---|---|---|
| 1 | **B+** | **STOP-THE-BLEED** (exposure structure + stop forensic + re-entry ban) | The only dossier anchored to tape-verified facts; risk-reducing by construction; shippable now |
| 2 | **B−** | **FLAG-OFF PORTFOLIO** (T2 wide promotion; watchlist crash fix) | The program's only live RCT fired its pre-registered promotion rule; one env line; rest of the shelf is mirages |
| 3 | C− | Costs / execution capture | Rigorous half refutes its own materiality: friction ≈ $2–4K/100 trades vs −$23K; entry chase nets *favorable* (−$1,003) |
| 4 | D+ | Regime-gated hold (rvol>100 × hour-9) | Most hostility-tested cell still standing, but threshold-mined, frozen-OOS fails, and the deployable estimand (the actual book, gated) **loses at every threshold** |
| 5 | D | The different game | The certain half is stop-the-bleed wearing a strategy hat; the novel half cites the retracted pilot |
| 6 | **F** | **Short-fade** (flip `EXEC_FADE_SHORT_ENABLED`) | **Physically unfillable: 0-for-94 live borrow attempts over 2 months** (71 NOT-SHORTABLE pre-rejects + 20 broker 422s, verified in `logs/fader_short_2026-*.log`); the paper API enforces real ETB |

## §3 — The decisive findings (each independently verified)

1. **The borrow wall is absolute.** The MomentumX-FaderShort task has run at 15:50 ET since 2026-05-02 as an unintentional measurement instrument: 94 short attempts, **zero fills**. The 5/5-consecutive-week-positive shadow short signal (`selection_study_*.json`, +0.62%→+2.48%/ticket) is *real as a signal and unmonetizable as a trade* — the market prices the fade into the borrow. Doc 262 already ruled it; doc 270 left retirement "pending." **The d215 live-short disaster (25 trades, PF 0.11) plus this closes shorting on this broker permanently.** The signal's only deployable form is avoidance (doc 261) — and even there, a hard red-flag entry block is **measured negative**: it would have deleted LASE (+$73,825, redflag 0.9), the account's biggest win ever.
2. **A log-verified vanished-stop bug class sits inside the book's worst trades.** PLSM 6/24: filled 2,672 @ $11.14 (~$29.8K, **14.8% of equity**) with its OTO stop priced at $11.15 — *above its own fill* — then `D231 RECON_HARD_BLOCK` 8s later; realized −$3,876, the single worst trade. `RECON_HARD_BLOCK STOP` appears in **26 session files**; 80+ D313 emergency-stop events. Log-verified excess loss ≈ $5,400 on just the two worst trades; the class-wide number is the most valuable unmeasured dollar figure in the system. **Part of the "strategy bleed" may be a stop-integrity bug.**
3. **The doc-266 T2 pre-registered promotion rule fired.** Extending the hardcoded window (`arm_broker_truth_doc266.py`) to the present: wide-stop arm −1.10% mean / +0.04% median (n=36) vs tight −2.75% / worse (n=42), out-of-sample past 6/10; both registered clauses pass. CI still spans zero ([−3.0, +5.8]pp) — the honest dollar credit is $0 until the n≥60 checkpoint — but the program's own discipline says: **a pre-registered rule that fires gets honored.** One env line, risk-reducing, instantly revertible.
4. **A measured re-entry-ban counterfactual: +$9,948 on the actual book, zero forgone winners** (banning re-entry into a ticker for 7 sessions after a realized loss skips 8 trades — all losses). Needs the broker-truth reconciliation (finding 2's forensic) to confirm attribution before flipping.
5. **The forward-measurement layer is silently broken.** The doc-255/261 red-flag collector froze 2026-06-08 (nightly NAType crash at `rocket_watchlist_engine_doc255.py:178`); the ps1 fails silent. The "live residue accumulating prospectively" hasn't been, for a month. `fade_short_realistic` was never computed. Instruments that would earn future offense are dark.
6. **What is formally retracted/dead after this dive:** the doc-201 "+9.47%, 100% win, n=11" fade-short pilot (doc 213's corrected n=66 re-run: **−1.65%**, cited-as-live for months in the config docstring); doc-264's overnight gate (refuted on data: −20.9%/ticket mean on the gated set); the red-flag hard entry block (kills LASE); EMPTY≠BEAR's "+$7.2K" (from-open pricing on a decision-time gate — the doc-233 mirage class verbatim); the dip-flatten "+$30,274" as justification (regime + dead-posture tainted); the rvol>100 gated hold as a "discovery" (demoted to candidate: mined thresholds, OOS-failed, book-negative); prior-day-rocket/SPY/IWM regime gates (both halves lose); every ceiling-arithmetic dollar claim.

## §4 — THE RECOMMENDED PLAY (staged; every live flip is Pierce's)

**STAGE 1 — before Monday's open (operator env lines; exposure-reducing; instant rollback):**
- **1a. The size package**: `EXEC_RISK_PER_TRADE_PCT=0.0067` and `EXEC_MAX_POSITION_PCT=0.05` (≈⅓ current sizing; both env names verified — `EXEC_MAX_POSITION_PCT` already exists in the operator env). Restoration is **pre-registered**: revert only when the nightly doc-280 posture scoreboard turns durably positive at n≥30 sessions — not on vibes. In-sample this is worth ≈ +$15.3K/32 sessions of avoided bleed; forward-credited as a bleed-rate cut, not alpha.
- **1b. The fired prereg rule**: `MOMENTUM_T2_WIDE_PCT=0.75` (75/25 keeps the RCT's control arm alive). Re-registered demotion: at n≥60/arm, if winsorized mean AND median flip to tight≥wide, revert. Dollar credit $0 until that checkpoint.

**STAGE 2 — week 1, the decider (read-only forensic, then one gated fix):**
- **2a. The vanished-stop class forensic**: for every clean-book trade, extract intended stop (D100/D106) vs `RECON_HARD_BLOCK`/D313 events vs realized exit; query the logged order ids (PLSM `6078f7ad`, RTB `96d3bc47`) to separate broker-cancel from recon-artifact; reconcile `trade_results.jsonl` against `broker_truth_recon` (incl. the 06-05 sign flip). **GATE A:** class-wide excess loss ≥ ~$8K *and* root cause = real stop loss → fix the stop path as the *only* close-path code change, behind the full gauntlet. Below the gate → no code ships.
- **2b. Loser-re-entry ban** (7-session ticker cooldown after a realized loss): built flag-OFF; Pierce flips only after 2a confirms the 0/8 attribution. Failure mode is trade-less.

**STAGE 3 — weeks 1–2, the $0-credited measurement layer (no live change; the only route back to offense):** fix the NAType crash + make the collector fail loud (future gate for any use: n≥300 forward rows, day-block CI excluding 0, ETB-fraction measured); populate exit-slippage instrumentation at the close call-sites; keep the frozen gated-hold candidate (rvol>100 × hour-9, hold-to-close, 15% stop, never overnight) as a **prereg-frozen forward ledger only**; run the full-denominator unfilled-cohort replay (211 verdicts → 56 fills; doc-265's number is tainted, the replay must include all 155 misses). **Retire the FaderShort submission path** (doc 262 rec #2) — repurpose the 15:50 runner as pure avoidance-signal logging.

## §5 — The honest 60-session EV

Baseline: **−$43K** (clean-book rate) to **−$71K** (broker-equity rate). The play credits **zero alpha** — it is bleed reduction plus one forensic recovery bet; the components draw from the same pool and do not stack additively. Defensible range: a **40–70% cut in the bleed rate → ≈ −$13K to −$26K**, i.e. **+$17K to +$45K vs status quo**. The only path that beats it is `MOMENTUM_HALT_NEW_ENTRIES=1` (+$43–71K, zero engineering) — the honest statement is that continuing to trade at reduced size is a *paid subscription to the experiment's forward instruments*, and the doc says so rather than dressing it as strategy.

## §6 — What would change the answer

A durably positive posture-delta scoreboard (regime turn) → restore size, revisit the gated hold from its frozen ledger. A broker with real locate service → reopen the short side from the 5/5-week shadow signal. The stop forensic finding ≥$8K of bug-class loss → the fix is the one code path worth touching. The repaired red-flag collector separating at n≥300 with ETB names → a sizing-downweight (not a block). Otherwise: the infra is the asset; the strategy remains the liability.

**Artifacts**: `data/research/doc281_state.md`, workflow `wf_72cee21b` (10 agents, transcripts in the session dir), verified log accounting cited inline. Live changes: none by the agent; Stage-1 env lines staged for Pierce.
