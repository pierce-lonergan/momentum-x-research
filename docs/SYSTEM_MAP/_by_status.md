# _by_status.md (derived — do not edit by hand)

Generated by `_linter.py`. DEPRECATED + REVERTED are delete-candidates / lessons-learned.
Generated at: 2026-05-25T01:05:25.772041+00:00

## ACTIVE (27 codes)

- **D101** (BIPOLAR_MFCS): Bipolar [-1, +1] MFCS range. Enables negative consensus expression and STRONG_BEAR signals to lower composite.
- **D116** (CONFIDENCE_FLOOR_020): Confidence floor 0.20 min for directional signals. Stabilizes MFCS under low-confidence agents.
- **D142** (PHASE1_STOP_TIGHTENING): Time-phased stop override: Phase 0 ultra-tight first 30s, Phase 1 1.5% static. LONGS only. Wide arm bypasses via D310.
- **D146** (NON_SHORTABLE_FILTERING): Filters non-shortable stocks before short entry. Enables D147 fix.
- **D147** (SHORTABILITY_CHECK): Skips stop protection for non-shortables (Alpaca 422 on partial sells); D165 compensates.
- **D163** (TRAILING_STOP_SOFTWARE): Per-cycle software trailing stop (fires at +2% gain); trail level = midpoint(entry, peak).
- **D165** (TRANCHE_PROFIT_TAKING): Software tranche exits at +3/+6/+10% (longs) or -3/-6/-10% (shorts). Bypasses Alpaca 422 on non-shortables.
- **D277** (HALT_NEW_ENTRIES): Operator kill switch. Refuses OTO + short submissions mid-session. Alert per refusal (post-D156 silent-block incident).
- **D278** (T1_NEXT_OPEN_EXIT_POLICY): Carry-overnight policy. Skips BAR-1 EXIT + TIME_EXIT, closes positions at next session market open via D91.
- **D281** (KELLY_BASELINE_N25): Per-tier Kelly baseline established at n=25+. Control measurement for D291 series ratchets.
- **D290** (ADAPTIVE_KELLY_BY_AUM): AUM-bracketed Kelly schedule. +60% $PnL at $500k bracket vs flat.
- **D291_5** (HIGH_TIER_KELLY_RAISE_50): HIGH tier Kelly cap 0.35 → 0.50. Trigger met: n_HIGH=56, mean return +4.13%.
- **D293a** (TABPFN_SINGLE_SEED_REVERT): Single-seed revert after D293 ensemble failed bootstrap CI gate. n_est=1, single random_state.
- **D294** (ALPACA_STOP_FLOOR_CLAMP): Stop-loss price clamp: enforces stop ≥ entry × (1 - max_pct) AND stop ≤ entry - $0.01 to avoid Alpaca rejection on sub-$1 ticks.
- **D295** (D91_NEXT_OPEN_JOURNAL_CLOSE): Records explicit journal close when D91 fires next-open exit. Prevents D222 EOD recon flagging carry-overnight closes as MISSING.
- **D297** (OTO_STOP_FILL_JOURNAL): OTO stop-leg fill records journal close. Closes the CISS/GCTS-class silent journal gap.
- **D304** (ALERT_KWARG_COMPLETENESS): Standardized Discord alert schema validation. All alerts must pass complete kwarg set (color, timestamp, footer) to prevent KeyError on post.
- **D308** (STOP_DECISION_LOG): JSONL shadow log of (atr_stop, phase1_stop, submitted) at every OTO submission. Best-effort; production unaffected on write failure.
- **D310** (STOP_WIDENING_T2_ARM_ASSIGN): Hash-keyed 50/50 A/B: wide arm uses ATR stop + halved qty + L1 standalone STOP submission; tight arm preserves D142 OTO.
- **D311** (PNL_RECON_AFTER_FILTER): Trade journal pulls broker fills with after=<today UTC midnight> filter. Prevents phantom $49k delta on EOD recon.
- **D312** (GATE_SUMMARY_TO_EOD): Per-session gate-rejection counter (D216, D85, etc.) surfaced via veto_summary on EOD Discord. Closes Friday silent-BUY visibility.
- **D313** (HEDGE_INTEGRITY_WATCHER): L2 invariant watcher: polls broker every 20s, enforces qty != 0 → has_protective_order. Emergency stop after 60s tolerance + L2_WATCHDOG.
- **D314** (DURABLE_ALERT_SPOOL): Every Discord alert spooled to data/alerts/ BEFORE live POST. Retry loop redelivers on failure. [STALE] prefix on >2h redeliveries.
- **D315** (BOOT_SELF_TEST): Single Discord embed at startup confirming T2/L2/L2_WATCHDOG/D314_RETRY status. Doubles as E2E test of durable_post pipeline.
- **D91** (LLM_FALLBACK_CHAIN): Three-tier model resilience: primary → fallback (different family) → emergency. Activated on timeout / API error / circuit breaker.
- **D92** (EMERGENCY_MODEL_TIER): Last-resort model (Llama 70B) when primary + fallback fail. Defensive <think> block parsing.
- **DXXX_EXAMPLE** (SHORT_LABEL / human-readable name): Brief one-line description for grepping.

## FILED (3 codes)

- **D293_8** (TABPFN_ENSEMBLE_RETEST): Re-test multi-seed ensemble at n >= 200 labeled picks. Targets ~2026-07-12 after T2 accumulates picks.
- **D316** (EXTERNAL_PROCESS_WATCHDOG): PowerShell scheduled task polls heartbeat.json freshness; restarts bot + Discord webhook if >3 min stale. Closes last in-process SPOF.
- **D317** (EVENT_SOURCED_TRADE_JOURNAL): SQLite/DuckDB append-only events. Crash recovery one-line replay. Target: 2026-06-15.

## DEPRECATED (2 codes)

- **D164** (EARLY_PROFIT_TAKE_DEPRECATED) → superseded by D165: Aggressive early exit trigger. Superseded by D165 software tranches.
- **D26** (WEIGHT_REDISTRIBUTION_LEGACY) → superseded by D101: Pre-D101 weight redistribution when agents returned empty signals. Replaced by bipolar [-1,+1] MFCS.

## REVERTED (1 codes)

- **D293** (TABPFN_ENSEMBLE): Multi-seed ensemble REVERTED 2026-05-12: bootstrap CI gate failed at n=2-7 dates. D293.8 filed for n>=200 retest.
